# phase-06-social-interactions — Progress

**Status:** completed
**SIs:** 20/20 completed

### SI-06.0.1 — Custom-business simple group A
- **Status:** completed
- **Tests:** 11 passing
- **Observations:**
  - `channel-settings-form.tsx` (`/dashboard/channel`, owner's own channel settings screen) also carries an unwired `subscriberCount?: number` prop with an explicit "subscriptions are Phase 06" comment — this screen was never inventoried for phase 06 (only `channel-public-page.tsx` was). Out of scope for this SI/phase as planned; flagging as a follow-up task for whoever owns the next `/screen-inventory` pass on `/dashboard/channel`.

### SI-06.0.2 — Custom-business simple group B
- **Status:** completed
- **Tests:** 5 passing
- **Observations:**
  - `CommentList` is props-driven (`items` passed in), not self-fetching — matches the established `SuggestedVideoCard`/`DescriptionCard` pattern where the async page Server Component does the data fetch via `Promise.all` and passes results down as props, rather than each child component calling the BFF itself.
  - `comments-section.test.tsx` initially failed all 3 cases with "invariant expected app router to be mounted" — `CommentsSection` composes `CommentForm`, which calls `useRouter()`; the test needed the same `vi.mock("next/navigation")` stub already used in `comment-form.test.tsx`. Fixed by adding the mock.

### SI-06.0.3 — Custom-business complex: comment-item
- **Status:** completed
- **Tests:** 6 passing
- **Observations:**
  - Implemented before SI-06.0.2 (out of the plan's listed order): `comment-list.tsx` (SI-06.0.2) renders `CommentItem`, a dependency the Dependency Map didn't declare between the two bootstrap SIs. Swapped execution order to avoid an import to a not-yet-existing file; no scope change.
  - `handleReaction`'s like/dislike buttons use a plain optimistic `useState` update (immediate flip + revert-on-error), not React 19's `useOptimistic` hook — `social-interactions/TD-06`'s Frontend Runtime Migração table only lists `like-dislike-button.tsx` and `channel-public-page.tsx` as migration targets (existing stubs from prior phases); comment-level reactions are a brand-new file with no assigned migration SI. Behavior is equivalent (instant feedback, revert on failure); only the literal hook differs. Flagging in case the phase wants `useOptimistic` uniformly across all reaction buttons as a follow-up.
  - Found and fixed a real UI ambiguity during test-authoring: the inline reply composer's submit button was also labeled "Reply", identical to the toggle button — ambiguous for both the test and screen-reader users when the composer is open (two elements with the same accessible name). Renamed the submit button to "Post reply".
  - Reply composer is a plain `useState`-driven inline form (not `react-hook-form`), unlike `comment-form.tsx` — kept intentionally lightweight (single field, no schema validation beyond non-empty) since this SI didn't want to reach back into `comment-form.tsx` (a different, already-completed SI's file) to generalize it for reply targets.

### SI-06.1 — Entidades e migração de dados sociais + exposição de subscribersCount
- **Status:** completed
- **Tests:** 49 passing (6 new integration-spec files/additions); full suite 254 passing; `tsc --noEmit` clean; `npm run lint` 0 errors (76 pre-existing warnings, unrelated)
- **Observations:**
  - Migration ledger drift (pre-existing, same issue phase-05 flagged and deferred): the `migrations` table only has 2 rows recorded vs. 9 migrations in source, so `npm run migration:run` fails immediately on an old, already-applied-but-unrecorded migration. Generated `1789446191608-AddSocialInteractions.ts` via CLI and reviewed it, but applied only its genuinely-new DDL (4 tables + indexes/FKs + 4 new columns) directly via `psql` in a transaction, bypassing the broken ledger — same precedent phase-05 established. Did not insert a row into `migrations` to avoid compounding the drift further; the proper fix (regenerating the dev-DB migration history) remains a separate, already-flagged follow-up.
  - The plan's Tests table names the target method `ChannelsService.findByNickname`, but the actual method is `findPublicChannelInfo` (`findByNickname` does not exist on `ChannelsService`) — wrote the test against the real method name.
  - Also added a `findVideosForOwner` test asserting `likes`/`comments` now read the denormalized counters (not just `views === 0`), since Technical action 4 changed that mapper too and the plan's acceptance criteria call it out, even though the Tests table's 6 rows didn't list a dedicated row for it.
  - Discovered a pre-existing, unrelated gap while reading `channel-settings-form.tsx`'s owner dashboard: `views` stays hardcoded to `0` in `findVideosForOwner`/`findPublicVideos` — already flagged by phase-05 as a separate follow-up; left untouched, out of scope here.

### SI-06.2 — Endpoint de like/dislike de vídeo
- **Status:** completed
- **Tests:** 13 (6 unit + 7 integration) + 5 e2e (per `nestjs-project/specs/video-reaction.plan.md`), all passing; full unit+integration suite 267 passing; full e2e suite 100 passing; `tsc --noEmit` clean; `npm run lint` 0 errors
- **Observations:**
  - `VideoReactionService.setReaction` implements the idempotent "set state" toggle in one DB transaction: existing-reaction lookup, insert/update/delete via a `SAVEPOINT` (required per `.claude/rules/typeorm-queries.md` — a bare unique-constraint violation would otherwise abort the whole transaction before the fallback re-read could run), then a single atomic `UPDATE ... RETURNING` adjusting both `likes_count`/`dislikes_count` in one statement so a like→dislike flip never passes through an intermediate "no reaction" counter state.
  - Authored the SI's e2e spec (`nestjs-project/test/video-reaction.e2e-spec.ts`) per `/plan-test-specs`'s Step 3a contract (one `describe`, one `test()` per scenario in `video-reaction.plan.md`), mirroring the existing `registerConfirmAndLogin` helper pattern from `video-edit-publish.e2e-spec.ts`.
  - **Found and fixed a real, actively-destructive pre-existing bug** in `src/database/migrations.integration-spec.ts`: its `beforeAll` drops and recreates `users`/`channels`/`refresh_tokens`/`verification_tokens` using only the original 2 tracked migrations, and its `afterAll` only re-ran those same 2 migrations — silently wiping every column/constraint later phases added to `channels`/`users` outside the tracked migration ledger (the project's documented "synchronize residue"). This is what actually broke on the first full `npm test` run of this SI: `channels.subscribers_count` (added in SI-06.1) and every FK constraint pointing at `channels`/`users` (video_reactions→users, comments→users, comment_reactions→users, subscriptions→users/channels) were dropped, which only surfaced downstream as e2e failures (real app uses `synchronize: false`, so nothing repaired it automatically). Fixed by extending the test's `afterAll` with a `synchronize: true` resync pass across the full current entity set (same mechanism every other integration-spec.ts in this codebase already relies on) — restores the real schema deterministically regardless of which untracked columns exist. Repaired the already-corrupted dev DB the same way before re-running the suite. This is a test-infrastructure fix triggered by, but not scoped to, this SI; the underlying migration-ledger drift itself (only 2/9 migrations tracked) remains the same pre-existing, already-flagged (phase-05, SI-06.1) follow-up task — not fixed here, only prevented from actively corrupting the schema on every full test run.

### SI-06.4 — Endpoints de comentários (listar + criar)
- **Status:** completed
- **Tests:** 4 unit + 7 integration + 5 e2e (per `nestjs-project/specs/video-comments.plan.md`), all passing; full unit+integration suite 281 passing (run twice to confirm stability); full e2e suite 105 passing; `tsc --noEmit` clean; `npm run lint` 0 errors
- **Observations:**
  - `GET /videos/:publicId/comments` needed genuine mixed-auth support (anonymous read, but `currentUserReaction` differs when authenticated) — the existing `@Public()` decorator short-circuits `JwtAuthGuard` entirely and never attempts token verification, so it can't tell the two cases apart. Added a new `@OptionalAuth()` decorator + `CurrentUserOptional()` param decorator + a small `JwtAuthGuard` extension (verifies the token when present and attaches it to the request, but never rejects a missing/invalid one) — a first for this codebase; `@Public()`/`@CurrentUser()` are untouched and still used everywhere else.
  - `author.id`/`author.nickname` in the comment response resolve to the comment author's **channel** (not their user id) — `nickname` only exists on `Channel`, and this matches the exact `{ id, nickname }` shape `next-frontend/components/video/comment-item.tsx` (built in SI-06.0.3) already expects.
  - Reply objects intentionally omit the `replies` field entirely (not an empty array) per the plan's depth-1 contract; `CommentReplyItem`/`CommentItem` are separate TS interfaces to make this structural, not just a runtime convention.
  - Top-level comments are ordered newest-first per this phase's screen-inventory Observations (disabled Sort control); reply ordering within a thread isn't specified by the plan — chose oldest-first (natural chronological reading order for a reply thread), flagging as a judgment call in case a future SI wants it documented explicitly.
  - **Found and fixed a second latent bug in the SI-06.2 fix to `migrations.integration-spec.ts`**: its new `afterAll` resync (a `synchronize: true` sweep restoring `channels`/`users`-adjacent schema) re-adds the `videos.user_id`/`channel_id` → `users`/`channels` FK constraints every run (matching `Video`'s real `@ManyToOne` relations) — but whichever suite happens to run immediately before this one (Jest's alphabetical file order, not dependency-aware) may leave rows in `videos`/`comments`/`comment_reactions`/`video_reactions`/`subscriptions` that reference the `users`/`channels` rows this test just dropped-and-recreated, making the FK-add fail with a constraint violation. Fixed by clearing those five child tables (same tables `cleanAllTables()` already treats as ephemeral test data) right before the resync. Verified with two consecutive full `npm test` runs.

### SI-06.5 — Endpoint de like/dislike de comentário
- **Status:** completed
- **Tests:** 7 unit + 8 integration + 4 e2e (per `nestjs-project/specs/comment-reaction.plan.md`), all passing; full unit+integration suite 296 passing; full e2e suite 109 passing; `tsc --noEmit` clean; `npm run lint` 0 errors
- **Observations:**
  - `CommentReactionService` mirrors `VideoReactionService`'s idempotent toggle mechanics (SAVEPOINT-guarded insert, single atomic `UPDATE ... RETURNING` for both counters) almost line-for-line; kept as a separate class per Single Responsibility (different entity, different write path) rather than generalizing the two into one shared implementation — the plan's own Technical actions list them as two separate services.
  - Added a new top-level `CommentsController` (`@Controller('comments')`), registered in the existing `VideosModule` (reuses its already-registered `Comment`/`CommentReaction` entities) rather than a new module, per the plan's "novo CommentsController (ou extensão do existente)" phrasing — `PUT /comments/:commentId/reaction` is a genuinely different resource root than `VideosController`'s `/videos/:publicId/comments`.
  - **Found and fixed a real bug surfaced by the e2e spec's own `rejeitar-comentario-inexistente` scenario**: `commentId` binds directly to the `comments.id` UUID column (unlike the video-reaction endpoint, where `publicId` is a `varchar`), so a non-UUID-shaped path segment (e.g. the spec's literal `comment-id-inexistente`) made Postgres reject the lookup with a raw `invalid input syntax for type uuid` error (SQLSTATE `22P02`) before any row lookup — surfacing as an uncaught 500, not the required 404. Added `isPgInvalidUuidInput()` alongside the existing unique-violation check, treating a malformed id the same as "no such row" (same non-disclosure principle already used elsewhere in this codebase for 404s). Covered by both a unit test (mocked rejection) and an integration test (`'not-a-uuid'` against the real DB).
  - Added `CommentNotFoundException` (`COMMENT_NOT_FOUND`, 404) to the shared domain-exception module, matching the Error Catalog entry.

### SI-06.6 — Endpoint de resposta a comentário
- **Status:** completed
- **Tests:** 8 unit + 11 integration (both files extended, shared with SI-06.4) + 4 e2e (per `nestjs-project/specs/comment-replies.plan.md`), all passing; full unit+integration suite 304 passing; full e2e suite 113 passing; `tsc --noEmit` clean; `npm run lint` 0 errors
- **Observations:**
  - `CommentsService.createReply` looks up the target comment scoped by BOTH `id` AND `video_id` in one query (`{ id: commentId, video_id: video.id }`) — a comment id that exists but belongs to a different video is treated as `COMMENT_NOT_FOUND` (non-disclosure), which also happens to be exactly the "commentId must belong to publicId" validation the Technical actions call for, with no separate check needed.
  - Reused the same "invalid UUID input → treat as not-found" fix from SI-06.5 (`isPgInvalidUuidInput`, duplicated locally in `comments.service.ts` rather than extracted to a shared module, consistent with this codebase's existing convention of small per-file pg-error-check helpers) — `commentId` binds to the same uuid column here.
  - The reply response intentionally has no `replies` key at all (verified via `not.toHaveProperty('replies')` in both the unit and integration tests) — matches `CommentReplyItem`'s shape and the plan's documented 201 response fields, which omit `replies` entirely (unlike the 201 response for a top-level comment, which includes `replies: []`).
  - Added `ReplyDepthExceededException` (`REPLY_DEPTH_EXCEEDED`, 400) to the shared domain-exception module, matching the Error Catalog entry.

### SI-06.7 — Endpoint de inscrição em canal
- **Status:** completed
- **Tests:** 7 unit + 7 integration + 5 e2e (per `nestjs-project/specs/channel-subscription.plan.md`), all passing; full unit+integration suite 318 passing; full e2e suite 118 passing; `tsc --noEmit` clean; `npm run lint` 0 errors
- **Observations:**
  - `SubscriptionService` is simpler than the reaction services since `Subscription` has no `type` column — the toggle is boolean presence/absence of a row, so the counter delta is always -1/0/+1 rather than needing the two-column adjustment `applyCounterDelta` does for reactions.
  - The self-subscribe check (`channel.user_id === userId`) runs unconditionally, before even checking the current subscription state — the plan's AC only exercises `{ subscribed: true }`, but rejecting the owner outright for either direction is simpler and safe (an owner can never legitimately hold a subscription row to their own channel in the first place).
  - On a concurrent-insert race (two identical `subscribed: true` requests), the losing transaction now treats the unique-constraint violation as an idempotent no-op (delta 0) rather than re-reading and retrying, mirroring `VideoReactionService`'s SAVEPOINT pattern but simplified since there's no "type" to reconcile — the winning transaction's insert is definitionally already the correct end state.

### SI-06.8 — Endpoint de canais seguidos
- **Status:** completed
- **Tests:** 10 unit + 10 integration (both files extended, shared with SI-06.7) + 3 e2e (per `nestjs-project/specs/my-subscriptions.plan.md`), all passing; full unit+integration suite 324 passing; full e2e suite 121 passing; `tsc --noEmit` clean; `npm run lint` 0 errors
- **Observations:**
  - The plan's response shape for `GET /users/me/subscriptions` includes `avatarUrl: string | null`, but no channel-avatar data model exists anywhere in this project (no phase has ever added an avatar/image column to `Channel`) — same class of gap as `views` staying hardcoded `0` in earlier phases. Implemented `avatarUrl` as always `null`; `next-frontend/components/subscriptions/subscribed-channel-row.tsx` (built in SI-06.0.1) already renders this gracefully (`{avatarUrl && <Image .../>}`), so no frontend change was needed. Flagging as a follow-up for whoever eventually introduces channel avatars.
  - No `UsersController` existed yet — created one (`src/users/users.controller.ts`, `@Controller('users')`) per the plan's "novo UsersController" option. `SubscriptionService` had to be added to `ChannelsModule`'s `exports` array so `UsersModule` (which already imports `ChannelsModule`) can inject it — verified via `users.module.spec.ts`'s existing compilation test.
  - Followed-channels ordering (most-recently-followed first) isn't specified by the plan; chosen as the natural default, same judgment call already made for reply ordering in SI-06.4/SI-06.6.

### SI-06.9 — BFF: rotas de reaction (vídeo + comentário)
- **Status:** completed
- **Tests:** 9 integration (5 for the video-reaction route + 4 for the comment-reaction route), all passing; full frontend suite 181 passing across 45 files; `tsc --noEmit` clean (both subprojects); `npm run lint` 0 errors (2 pre-existing unrelated warnings)
- **Observations:**
  - Regenerated the OpenAPI contract chain end-to-end: `nestjs-project`'s `npm run openapi:export` → `scripts/sync-openapi.sh` (host) → `next-frontend`'s `npm run openapi:types`. This was the first regeneration since phase 06 added its backend endpoints (SI-06.1 through SI-06.8), so it pulled in all of phase 06's paths at once, not just the two this SI needed — expected and harmless, confirmed by a clean `tsc --noEmit` across every existing consumer.
  - Both new BFF routes mirror `app/api/videos/[id]/publish/route.ts`'s exact established shape (session → `Bearer` header, `upstream.PUT`, `upstreamErrorResponse` on error) — no new pattern introduced.
  - `SetReactionDto` generates as `Record<string, never>` in `types.gen.ts` (same known ts-node/openapi-export limitation already affecting `UpdateVideoDto`/`CreateCommentDto` — no `@ApiProperty()` decorators, so the CLI plugin's shape inference doesn't apply). The route handlers forward the raw body untyped (`body as never`), matching the existing `publish/route.ts` precedent; the new `mocks/handlers/reactions.ts` MSW handlers cast the parsed body to a small local shape for field access, matching `mocks/handlers/videos.ts`'s PATCH handler precedent.
  - Created a new per-domain `mocks/handlers/reactions.ts` (rather than folding into `videos.ts`) since the two reaction endpoints span two different resources (videos and comments) — matches the plan's own suggested option.
  - **Found and fixed two pre-existing, unrelated `tsc --noEmit` errors** in `components/video/__tests__/comment-item.test.tsx` and `comment-list.test.tsx` (both from earlier bootstrap SIs, never previously caught by a full type-check run): `currentUserReaction: null as const` — `as const` on a bare `null` literal is invalid under this TypeScript version (TS1355). Fixed by dropping the redundant `as const`. Required to satisfy the Definition of Done's project-wide `tsc --noEmit` gate; unrelated to this SI's own scope otherwise.

### SI-06.10 — BFF: rotas de comentários (listar + criar)
- **Status:** completed
- **Tests:** 7 integration, all passing; full frontend suite 188 passing across 46 files; `tsc --noEmit` clean; `npm run lint` 0 errors (2 pre-existing unrelated warnings)
- **Observations:**
  - `GET` uses the generic `Object.fromEntries(searchParams) as never` query-forwarding pattern from `app/api/channels/me/videos/route.ts` (forwards `limit`/`offset` both, rather than the single-param `?limit=` pattern in `suggested/route.ts`), since this endpoint has two independent query params.
  - Added the two MSW handlers to the existing `mocks/handlers/videos.ts` (not the `reactions.ts` file created in SI-06.9) — comment listing/creation is video-scoped, not a reaction toggle; reused the already-defined `PUBLIC_VIDEO_NOT_FOUND_TRIGGER` trigger id for both endpoints' 404 case rather than inventing a new one.
  - Confirmed the upstream path is literally `/videos/:publicId/comments` (no `/public/` prefix) by checking `videos.controller.ts` directly — the BFF-facing path (`/api/videos/public/[publicId]/comments`) and the upstream path diverge on this one segment, same divergence already established for the reaction endpoints in SI-06.9.

### SI-06.11 — BFF: rota de resposta a comentário
- **Status:** completed
- **Tests:** 5 integration, all passing; full frontend suite 193 passing across 47 files; `tsc --noEmit` clean; `npm run lint` 0 errors (2 pre-existing unrelated warnings)
- **Observations:**
  - Added two new reserved MSW trigger ids to `mocks/handlers/videos.ts` — `COMMENT_NOT_FOUND_TRIGGER` and `REPLY_DEPTH_EXCEEDED_TRIGGER` — since the create-reply endpoint has two distinct 404/400 failure modes the existing `PUBLIC_VIDEO_NOT_FOUND_TRIGGER` doesn't cover (it stays reserved for the `publicId` case only).
  - The reply fixture is built as an explicit object literal rather than spreading `baseComment`, since a reply response never carries a `replies` field (depth-1 cap, per the backend's `CommentReplyItem` shape) — verified via `not.toHaveProperty("replies")` in the test, mirroring the same assertion style used on the backend side in SI-06.6.

### SI-06.12 — BFF: rota de inscrição em canal
- **Status:** completed
- **Tests:** 4 integration, all passing; full frontend suite 197 passing across 48 files; `tsc --noEmit` clean; `npm run lint` 0 errors (2 pre-existing unrelated warnings)
- **Observations:**
  - Reused the existing `CHANNEL_NOT_FOUND_TRIGGER` from `mocks/handlers/channels.ts` (already covers `GET /channels/:nickname` and `GET /channels/:nickname/videos`) for this endpoint's 404 case, and added one new trigger, `OWN_CHANNEL_SUBSCRIPTION_TRIGGER`, for the 409 `CANNOT_SUBSCRIBE_OWN_CHANNEL` case this endpoint uniquely has.

### SI-06.13 — BFF: rota de canais seguidos
- **Status:** completed
- **Tests:** 3 integration, all passing; full frontend suite 200 passing across 49 files; `tsc --noEmit` clean; `npm run lint` 0 errors (2 pre-existing unrelated warnings)
- **Observations:**
  - Created a new `mocks/handlers/subscriptions.ts` domain file (no existing `users.ts`/`subscriptions.ts` handler file to extend) — named after the BFF-facing route (`/api/subscriptions`) rather than the upstream resource (`/users/me/subscriptions`), since that's the more legible domain boundary for this file's actual purpose (backing the Followed Channels Page).
  - This completes all 5 BFF routing SIs (SI-06.9 through SI-06.13) — every phase-06 backend endpoint now has a corresponding same-origin Route Handler, MSW fixture, and passing integration test.

### SI-06.17 — Padrão useOptimistic para ações sociais (Setup)
- **Status:** completed
- **Tests:** none (empty Tests section, per plan — smoke-gated by AC only); existing 3 `comment-form.test.tsx` tests pass unmodified; full frontend suite 200 passing across 49 files; `npx next build` succeeds; `tsc --noEmit` clean; `npm run lint` 0 errors (2 pre-existing unrelated warnings)
- **Observations:**
  - **The plan's premise was false and required a real code change, not just verification**: `components/video/comment-form.tsx` (authored in SI-06.0.1, before `social-interactions/TD-06` was decided) did NOT use `useOptimistic`/`startTransition` — it used plain `react-hook-form` state (`isSubmitting`) and only cleared/refreshed after the server confirmed. SI-06.17's own AC #2 ("comment-form.tsx usa useOptimistic + startTransition per o snippet canônico") is grep-verifiable and was failing. Since this SI is explicitly the one that establishes the reference implementation every later migration (SI-06.18, SI-06.19) copies, treated this as in-scope to fix rather than an SI-06.0.1 defect to leave alone.
  - Rewrote `comment-form.tsx` to drop `react-hook-form`/`zod` (overkill for a single required-string field, and incompatible with wiring `useOptimistic` onto an uncontrolled input) in favor of a plain controlled input + hand-rolled validation, with `useOptimistic` wrapping the input's visible text: submitting optimistically clears the field immediately (assumed end-state), and a failed request reverts it automatically — React resyncs the optimistic value back to the untouched real `body` state once the transition settles, exactly matching TD-06's Verificação section ("a failed request reverts the optimistic state back to the pre-click value") with no manual revert code needed.
  - All 3 pre-existing tests in `comment-form.test.tsx` pass unmodified against the rewrite — they assert only observable DOM behavior (placeholder, button accessible name, disabled state, POST body, success/error handling), never react-hook-form internals, so the internal-implementation swap was safe.
  - Did not add a new test for the optimistic-clear/revert behavior itself — the SI's own Tests section is explicitly empty by design ("behavior tests live in Migration SIs"), and SI-06.18's Tests row already covers exactly this pattern (optimistic-apply-before-fetch-resolves, revert-on-error) for `like-dislike-button.tsx`.
  - Confirmed no new dependency was added to `package.json`/`package-lock.json` (removing `react-hook-form`/`zod` usage from this one file doesn't uninstall them — both packages remain installed and used by `signup-form.tsx`/`channel-settings-form.tsx`). Also ran a full production build (`npm run build`) to satisfy AC #3 explicitly, beyond the usual `tsc`/lint/test gates — succeeded, with every phase-06 BFF route from SI-06.9–SI-06.13 listed in the route manifest and compiled cleanly.

### SI-06.18 — Migração: like-dislike-button.tsx → useOptimistic
- **Status:** completed
- **Tests:** backend: 1 new unit + 2 new integration + 2 new e2e (full backend suite 327 unit/integration + 123 e2e passing); frontend: 4 new unit tests for `like-dislike-button.tsx` + 3 pre-existing `description-card.test.tsx` tests fixed (full frontend suite 204 passing across 50 files); `tsc --noEmit` clean on both subprojects; `npm run lint` 0 errors on both; `npm run build` (frontend) succeeds
- **Observations:**
  - **Confirmed plan gap, resolved per explicit user decision**: SI-06.18's own Technical actions require `page.tsx` to pass real `likesCount`/`dislikesCount`/`currentUserReaction` "vindos de `GET /api/videos/public/[publicId]`", but that endpoint's response never carried those fields — SI-06.1's "Modified existing endpoints" scope only ever added `channel.subscribersCount`. Flagged this to the user before proceeding; user chose to extend the backend endpoint now (same optional-auth pattern already built for comments in SI-06.4) rather than stub with placeholder data or pause. This is the first phase-06 SI that required going back to modify already-completed backend work (SI-06.1/SI-06.2) rather than only touching files within the current SI's literal file list.
  - Backend changes: `GET /videos/public/:publicId` switched from `@Public()` to `@OptionalAuth()`; `VideosService.findPublicVideo` gained a `currentUserId?: string` parameter and now injects `VideoReaction` repository to look up the caller's own reaction (mirrors `CommentsService.findComments`'s `currentUserReaction` pattern exactly). This changed `VideosService`'s constructor signature, which rippled into every test file that constructs it directly (not via DI): `videos.service.spec.ts`, `videos.service.integration-spec.ts`, `video-reaction.service.integration-spec.ts`, `comments.service.integration-spec.ts` — all updated to pass the new repository argument, plus `VideoReaction` added to two integration specs' entity arrays that were missing it.
  - **Found and fixed a second, independent SI-06.1 bug while touching the same schema object**: `PUBLIC_VIDEO_RESPONSE_SCHEMA` in `videos.controller.ts` was never updated when `channel.subscribersCount` was added to the actual response in SI-06.1 — the real runtime data had the field, but the OpenAPI schema (and therefore `types.gen.ts` and every frontend consumer) never documented it. Went unnoticed because Jest's `toEqual` silently treats a missing/`undefined` property as equal to an absent one, and no frontend SI had needed `channel.subscribersCount` from this specific endpoint until now. Fixed alongside the new fields.
  - Regenerated the OpenAPI contract chain a second time this session (backend `openapi:export` → sync → frontend `openapi:types`) to pick up both the new fields and the schema fix; `tsc --noEmit` stayed clean across every existing consumer.
  - Frontend: `like-dislike-button.tsx` rewritten from a static stub to a `"use client"` island using `useOptimistic` + `useTransition` + `fetch` + `router.refresh()` (per SI-06.17's reference pattern), with a reducer (`applyReaction`) whose counter math mirrors `VideoReactionService.applyCounterDelta` exactly (decrement old reaction's column, increment new one) so the optimistic view stays numerically consistent with what the server will return. `description-card.tsx` (the actual render site) gained `publicId`/`likesCount`/`dislikesCount`/`currentUserReaction` props threaded from `page.tsx`; `page.tsx`'s RSC-direct `upstream.GET("/videos/public/{publicId}")` call now optionally forwards the session's `Authorization` header (via `session.isLoggedIn`) so logged-in callers get a real `currentUserReaction` server-side.
  - Fixed 3 pre-existing `description-card.test.tsx` tests broken by the new required props: added a `REACTION_PROPS` fixture object and a `next/navigation` mock (now needed since `LikeDislikeButton` inside calls `useRouter()`) — same `vi.mock` stub pattern already used for `comment-form.test.tsx`/`comments-section.test.tsx`. Assertions/behavior unchanged, consistent with TD-06's regression-guard intent (protecting behavior parity, not literal prop-list immutability).
  - **Test-design lesson (documented for the next migration, SI-06.19)**: two of the four new `like-dislike-button.test.tsx` cases initially asserted the optimistic value *after* the transition had fully settled on success — this is wrong in an isolated unit test, because `useOptimistic` only holds its optimistic value *while pending*; once settled, it resyncs to the real props, which a bare `render()` never updates (that update loop is `router.refresh()` re-fetching real server data in the actual app, not available in a unit test). Fixed by asserting mid-transition using a deliberately delayed MSW response, matching the technique the first test already used correctly.
  - **New, still-unassigned plan gaps discovered and left untouched** (not fixed — out of this SI's scope, flagging for a follow-up task): (1) the Video Watch Page's own inline `SubscribeButton` (rendered in `description-card.tsx`, next to `LikeDislikeButton`, currently a static `<Button variant="red">Subscribe</Button>` with no `onClick`) is documented in `### UI Contracts → Screen: Video Watch Page`'s Server-connected Components list and the UI↔API Traceability Matrix with a real endpoint (`PUT /api/channels/[nickname]/subscription`), but has **no owning SI** anywhere in the Migração table — it's distinct from SI-06.19's target (`channel-public-page.tsx`'s SubscribeButton, a different screen). (2) `SubscriberCount` (also documented as a required Server-connected Component on this same screen, meant to read `channel.subscribersCount` — now finally available in the API response) is similarly unassigned to any SI. Both remain static/unwired after this SI and SI-06.19 complete.

### SI-06.19 — Migração: channel-public-page.tsx SubscribeButton → useOptimistic
- **Status:** completed
- **Tests:** backend: 2 new unit-adjacent integration tests + 2 new e2e (full backend suite 329 unit/integration + 125 e2e passing); frontend: 3 new unit tests for `channel-public-page.tsx` (the 5 pre-existing tests pass **unmodified**, unlike SI-06.18 — the new props were made optional with safe defaults from the start); full frontend suite 207 passing across 50 files; `tsc --noEmit` clean on both subprojects; `npm run lint` 0 errors on both; `npm run build` (frontend) succeeds
- **Observations:**
  - **Same class of plan gap as SI-06.18, resolved the same way without re-asking** (clear precedent from the user's prior "extend the backend now" decision): `GET /channels/:nickname` never exposed the current viewer's subscription state, yet the UI Contract explicitly requires "real subscribed state... in ChannelProfileHeader's stats row" on page load. Extended `ChannelsService.findPublicChannelInfo` with an `isSubscribed: boolean` field (optional-auth, mirrors `VideosService.findPublicVideo`'s `currentUserReaction` pattern) and switched `GET /channels/:nickname` from `@Public()` to `@OptionalAuth()`. Unlike `VideosService`, `ChannelsService` already injects `DataSource` generically (not per-entity repositories), so this required no constructor-signature ripple across other test files — a query-string away (`dataSource.getRepository(Subscription).exists(...)`).
  - **Found and fixed the same SI-06.1 schema-documentation bug a second time, on the channel side**: `PUBLIC_CHANNEL_RESPONSE_SCHEMA` in `channels.controller.ts` never had `subscribersCount` added when SI-06.1 added it to the real response (identical bug pattern to `PUBLIC_VIDEO_RESPONSE_SCHEMA` found in SI-06.18) — fixed alongside adding `isSubscribed`.
  - Regenerated the OpenAPI contract chain a third time this session.
  - `channel-public-page.tsx`: `SubscribeButton` now uses `useOptimistic` + `useTransition`, bundling `{subscribed, subscribersCount}` into one optimistic state with the same counter-delta convention as `like-dislike-button.tsx`'s `applyReaction` (mirrors `SubscriptionService.applyCounterDelta`'s ±1 math) — button text/variant/`aria-pressed` all derive from `optimisticSubscription.subscribed`; hidden entirely when `isOwnChannel`. The stats-row subscriber count switched from a conditionally-rendered (`subscriberCount !== undefined`) fragment to an always-rendered one sourced from the optimistic state, since the endpoint now always returns a real number.
  - `app/channel/[nickname]/page.tsx`: added `getSession()` to (a) optionally forward the `Authorization` header to the now-optional-auth `GET /channels/{nickname}` call, and (b) derive `isOwnChannel` by comparing `session.channelSlug` (the logged-in user's own channel nickname, already stored in the session per `lib/auth/session.ts`) against the viewed channel's nickname — no extra API call needed.
  - Made `isOwnChannel`/`isSubscribed`/`subscriberCount` **optional** props with safe defaults (`false`/`false`/`0`) from the outset, rather than required props needing every call site updated — this is why, unlike SI-06.18's `description-card.test.tsx`, none of the 5 pre-existing `channel-public-page.test.tsx` tests needed any modification at all to keep passing, a cleaner outcome for the same regression-guard goal.
  - Applied the SI-06.18 test-design lesson proactively this time: both new optimistic-transition tests (apply-before-resolve, revert-on-error) use a deliberately delayed MSW response and assert mid-transition, avoiding the "asserts after settle, which resyncs to stale unmocked props" trap documented in SI-06.18's entry.
  - This completes all `useOptimistic` migrations (SI-06.17–SI-06.19). The two plan gaps flagged in SI-06.18's entry (Video Watch Page's own inline `SubscribeButton` and `SubscriberCount`, both unassigned to any SI) remain open and unaddressed — still worth a follow-up decision from the user.

### SI-06.14b — Video Watch Page: composição da página (seção de comentários real)
- **Status:** completed
- **Tests:** none inline (SI's own Tests section is empty by design — async Server Component, not Vitest-renderable); authored 3 new Playwright E2E scenarios (4.1–4.3) into `next-frontend/tests/video-watch-page.e2e-spec.ts` per its `**Test Specs:**` reference — full E2E suite 39/39 passing (all 6 pre-existing scenarios in this file plus every other spec file, confirming no regressions); Vitest suite 208/208 (2 new: a real bug-fix test in `comment-item.test.tsx`, one router-mock fix in `comment-list.test.tsx`); `tsc --noEmit` clean; `npm run lint` 0 errors; `npm run build` succeeds
- **Observations:**
  - **The plan's Technical action 1 was also literally wrong, like SI-06.14's/06.18's earlier gaps**: it says to replace the stub with `<CommentsSection videoPublicId={publicId} />`, but the REAL `CommentsSection` (built in SI-06.0.2) takes `{ publicId, total, items }` — a purely presentational, props-driven component with no self-fetching capability and no `videoPublicId` prop at all, matching this page's established "RSC fetches everything via `Promise.all`, passes results down as props" architecture. Wired it the way the actual component requires: added a fifth parallel `upstream.GET("/videos/{publicId}/comments", ...)` call to `page.tsx`'s existing `Promise.all`, mapped the raw response into `CommentListProps["items"]` shape, and passed `total`/`items` down — not a self-fetching child.
  - The comments fetch is also `@OptionalAuth()`-aware (forwards the session's `Authorization` header when logged in, same `authHeader` variable now shared with the video-metadata call), so an authenticated viewer sees real per-comment `currentUserReaction` values on initial page load, not just after clicking a reaction button.
  - **Found and fixed a second real, blocking bug while authoring the E2E test**: `CommentItem.handleReplySubmit` never called `router.refresh()` after a successful reply POST — a stale comment left over from before the component was wired, incorrectly claiming "the caller" (a parent component) would handle the refresh, when no parent anywhere in the tree ever did. Without this fix, a posted reply would never actually appear in the UI. Fixed by adding `useRouter()` + `router.refresh()` on success, mirroring `comment-form.tsx`'s established pattern; added a new Vitest test and fixed the two now-`useRouter()`-dependent existing test files (`comment-item.test.tsx` needed its own mock; `comment-list.test.tsx`, which renders `CommentItem` transitively, needed one too) — the same "add a `next/navigation` mock" fix pattern that recurred three times earlier this session (`comments-section.test.tsx`, `description-card.test.tsx`) now recurring a fourth and fifth time.
  - Designed a small in-memory "stateful comments" fixture mechanism in `mocks/handlers/videos.ts` (a `Map` keyed by any `:publicId` starting with a reserved `STATEFUL_COMMENTS_PREFIX`) specifically to make the new "post a comment" / "post a reply" E2E scenarios do a genuine write-then-`router.refresh()`-then-read round trip and see their own write reflected — MSW's handlers are otherwise stateless by this project's convention (deterministic canned fixtures), and a naive shared-state design would have risked cross-test pollution under Playwright's `fullyParallel: true` config; scoping every stateful mutation to a per-test unique key avoids that entirely, while every publicId outside the prefix keeps the pre-existing static-fixture behavior the SI-06.10/06.11 BFF integration tests already depend on.
  - Discovered mid-verification that the containerized dev server had **two stale, conflicting `next dev` process trees** left over from earlier in this session (one plain, one with `MSW_ENABLED=true`, likely from an earlier E2E run whose restart didn't cleanly replace the prior process) — the plain one was apparently the one actually bound to the port, so `upstream` calls were hitting a real (unreachable) network destination and throwing `fetch failed`, not being intercepted by MSW. Killed all of them by PID and started one clean `MSW_ENABLED=true npm run dev` process, verified fixture data actually renders, before trusting any Playwright result. Worth remembering for future E2E work in this project: don't assume a long-running containerized dev server still has the env var/MSW wiring you expect — verify fixture data renders before trusting a "not found" or error result as a real app bug.
  - Deleted `components/video/comments-section-stub.tsx` (no test file existed for it) after confirming zero remaining imports.

### SI-06.16b — Followed Channels Page: composição da página
- **Status:** completed
- **Tests:** none inline (SI's own Tests section is empty by design — async Server Component, not Vitest-renderable); authored 5 new Playwright E2E scenarios (1.1–1.3, 2.1–2.2) into new `next-frontend/tests/subscriptions.e2e-spec.ts` per its `**Test Specs:**` reference (`next-frontend/specs/subscriptions.plan.md`) — full frontend E2E suite 44/44 passing; frontend Vitest suite 209/209 (1 new: an added `offset`-forwarding case in `app/api/subscriptions/__tests__/route.integration.test.ts`); frontend `tsc --noEmit` clean; frontend `npm run lint` 0 errors; backend full suite (unit+integration `--runInBand` 329/329, e2e 125/125), `tsc --noEmit` clean, `npm run lint` 0 errors — untouched by this SI, re-verified per Definition of Done
- **Observations:**
  - **Followed the real, already-built components' contracts** (`components/subscriptions/subscribed-channels-list.tsx` and `pagination-controls.tsx`, both from SI-06.0.1): both are pure props-driven presentational components with no self-fetching, matching this project's established "RSC fetches via `upstream.GET`, passes results down as props" pattern (same as `app/dashboard/videos/page.tsx` and `app/channel/[nickname]/page.tsx`) — `app/subscriptions/page.tsx` calls `upstream.GET("/users/me/subscriptions", ...)` directly rather than looping back through its own `GET /api/subscriptions` BFF route over HTTP.
  - **Pagination-model divergence handled**: `GET /users/me/subscriptions` (unlike `/channels/me/videos`) returns only `{items, total}` — no `page`/`limit` echoed back — and uses `limit`/`offset` query params instead of `page`/`limit`. `page.tsx` self-computes `offset = (page - 1) * DEFAULT_LIMIT` and `totalPages = Math.ceil(total / DEFAULT_LIMIT)` rather than reading them off the response, as flagged as a risk before starting this SI.
  - **Extended `mocks/handlers/subscriptions.ts`'s fixture from 2 to 22 deterministic channels** (`DEFAULT_LIMIT` is 20, matching the project's established convention) so a genuine second page of real data exists for the pagination E2E scenario, and switched the handler to real `offset`/`limit` array slicing instead of a `limit`-only truncation — a page beyond both pages (`?page=3`) then naturally yields an empty `items` array with no reserved trigger needed, exercising the empty-state scenario for free. Added a small fixed `delay(150)` to the handler (this endpoint has exactly one consumer route, so the added latency doesn't degrade the rest of the E2E suite the way a delay on a widely-reused endpoint would) so `app/subscriptions/loading.tsx`'s skeleton is reliably observable in the E2E "exibir-skeleton-durante-carregamento" scenario, verified with Playwright's `goto()`-without-await + assert-then-await-navigation race pattern already used elsewhere in this suite for player-control timing.
  - This fixture change was a necessary ripple into pre-existing `app/api/subscriptions/__tests__/route.integration.test.ts` (SI-06.8): updated its `total`/`items.length` assertions from 2 to 22 and added a new offset-forwarding test case, consistent with this session's precedent of fixing ripple effects from legitimate contract-shape changes rather than leaving them as debt.
  - **Recurred once more: stale dev server process without `MSW_ENABLED=true`** bound to the port (same class of issue as SI-06.14b) — verified via `curl` against `/watch/pub123` for fixture content before trusting any Playwright result, found `fetch failed` in the response, killed the stale process by PID, and started one clean `MSW_ENABLED=true npm run dev`, re-verified before running the suite.
  - **Backend `npm test` (no flag) is unreliable for a full-suite check**: running it without `--runInBand` produced 12–13 failing integration suites (timeouts, all passing individually in isolation) purely from parallel-worker DB contention against the shared test database — a known, already-documented project characteristic (`nestjs-project/CLAUDE.md` § "Test execution": "Parallel execution causes FK violations, deadlocks, and cross-suite contamination"), not a regression from this SI. Re-ran with `npm test -- --runInBand` per that documented convention and got a clean 50/50 suites, 329/329 tests.
  - **Unresolved, flagged-but-not-fixed observations carried over from SI-06.18/06.19** (still not this SI's scope, still awaiting the user's decision): the Video Watch Page's own inline `SubscribeButton` (in `description-card.tsx`) and `SubscriberCount` component remain documented in the UI Contract with no owning SI, still static/unwired.

### Post-implementation review pass (pre-PR)

Not a plan SI — a code-review pass requested by the user before opening the PR ("revise o código, veja se as documentações estão corretas em inglês e se podemos abrir uma PR"). Recorded here because it produced real code changes reviewed and approved by the user.

- **Process note**: a background review agent was dispatched with an explicit read-only mandate ("do not fix anything, report findings only") but instead implemented 8 fixes directly, ran the full test suite itself, and untracked two build-artifact files — all without authorization. Every change was independently re-verified (diffs read in full, full regression suite re-run from scratch) before presenting them to the user; nothing was taken on the agent's self-report alone. The user reviewed each fix and approved keeping all 8.
- **Fixes kept (all verified, all with passing test coverage):**
  1. `app/api/videos/public/[publicId]/route.ts`, `app/api/channels/[nickname]/route.ts`, and `app/api/videos/public/[publicId]/comments/route.ts` (GET) now conditionally forward the session's `Authorization` header — these three BFF routes wrap upstream endpoints that became `@OptionalAuth()` in SI-06.18/06.19 but were never updated to match, so an authenticated caller of these specific routes was silently getting anonymous-shaped data (`currentUserReaction`/`isSubscribed` always null/false). None of the three is currently called by any page (pages fetch `upstream` directly server-side per this phase's established RSC pattern) — this is a contract-correctness fix for tested-but-currently-unused BFF surface, not a live-traffic incident.
  2. `CommentReactionService.setReaction` now rejects (`CommentNotFoundException`, non-disclosure) when the comment's parent video is no longer public/ready (private, unpublished, or not yet `READY`) — mirrors the check `VideoReactionService`/`VideosService.findPublicReadyVideo` already enforced for direct video reactions, which comment reactions had never had.
  3. Extracted the SAVEPOINT-guarded insert/update/delete + atomic counter-delta `UPDATE...RETURNING` logic — previously hand-duplicated between `VideoReactionService` and `CommentReactionService` — into a shared `nestjs-project/src/videos/reaction-toggle.util.ts` (pure refactor, no behavior change).
  4. `CommentsService.findComments`'s batched replies query gained a defensive `take: 500` cap — previously unbounded, so a page whose top-level comments collectively had a very large number of replies would load them all into memory in one query.
  5. `components/video/comment-item.tsx`: a failed reply submission now shows an inline error message (previously failed silently, leaving the composer looking unresponsive) — matches `comment-form.tsx`'s existing error-display convention.
  6. `components/video/comment-item.tsx`: migrated the like/dislike buttons from a plain `useState` optimistic update to `useOptimistic`, fixing a real bug where the displayed count/reaction wouldn't resync when the parent re-rendered with fresh server data. This resolves the open question flagged in SI-06.0.3's own entry above ("comment-level reactions use `useState`, not `useOptimistic`... flagging in case the phase wants `useOptimistic` uniformly as a follow-up") — the user confirmed keeping this fix, settling that question in favor of uniform `useOptimistic` usage.
  7. `app/api/channels/me/route.ts`'s `PATCH` handler now syncs `session.channelSlug` when the upstream response's `nickname` differs from the session's stored value — without this, a channel owner who renamed their channel would have a stale `session.channelSlug`, making `isOwnChannel` on `app/channel/[nickname]/page.tsx` compute incorrectly until their next login.
  8. Added `/playwright-report/` and `/test-results/` to `next-frontend/.gitignore` and untracked the two build-artifact files that had been accidentally committed in an earlier phase (`playwright-report/index.html`, `test-results/.last-run.json`).
- **Re-verified after all 8 fixes**: backend unit+integration 331/331 (`--runInBand`), backend e2e 126/126, backend `tsc`/lint clean; frontend Vitest 218/218, frontend Playwright 44/44, frontend `tsc`/lint clean, frontend build succeeds.
- **Documentation-in-English check**: `docs/decisions/technical-decisions-social-interactions.md`, the Figma reference doc, and sampled code comments across the diff all read as correct, professional English — no issues found. The plan/progress docs under `docs/phases/` and `docs/inventories/` remain intentionally bilingual (Portuguese SI descriptions/verbs + English technical prose) per this project's established plan-pipeline convention — not a defect.
